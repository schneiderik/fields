var Field = require('../../src/field');
var SpecHelper = require('../support/specHelper');

describe( 'Field', function() {
  describe( 'el', function() {
    it( 'references all DOM elements with the same [name] attribute', function() {
    });
  });
});
